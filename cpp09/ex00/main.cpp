#include "BitcoinExchange.hpp"

int main(int argc, char **argv)
{
	// 入力ファイルを読み取る
		// 入力ファイルが存在しない、読み取れない場合エラー出力して終了

	// パイプで区切る
		// パイプが存在しない場合はエラー出力して次の行
		// 数値がintの正の数に収まらない場合、エラー出力して次の行

	// 日付でdatabase内部の検索
		// 存在する日付はそのまま為替値を取得
		// 存在しない場合は一番近い日付（未来に寄せる？）を取得

	// 掛け算する
	// 出力する

	// ここで使用したコンテナはあとの課題で使えないためよく考えること

	return (0);
}
